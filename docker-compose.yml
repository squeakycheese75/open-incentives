services:
  api:
    build: .
    ports:
      - "8081:8080"
    environment:
      DATABASE_PATH: /data/app.db
      DATABASE_SEED: "auto"
      SERVER_JWT_SECRET: "APIKEY123"
    volumes:
      - ./data:/data